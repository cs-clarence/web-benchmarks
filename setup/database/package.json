{
    "name": "@repo/database",
    "version": "0.0.0-semantically-released",
    "scripts": {
        "image:build": "with-env 'docker build -t ${DOCKER_IMAGE_PREFIX}database:latest ./ -f ./Dockerfile'",
        "image:push": "with-env 'docker push ${DOCKER_IMAGE_PREFIX}database:latest'"
    }
}